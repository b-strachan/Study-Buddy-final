services:
  api:
    build: .
    container_name: study_buddy_api
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./app_data:/app/data
    extra_hosts:
      - "host.docker.internal:host-gateway"
